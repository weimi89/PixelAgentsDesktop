name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend:
    name: Frontend (type-check + test + build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npm run vite:build
      - run: npx tsc --noEmit
      - run: npm test

  sidecar:
    name: Sidecar (build + IPC smoke test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: node scripts/build-sidecar.mjs
      - name: Smoke test IPC
        run: |
          output=$(echo '{"id":1,"method":"getStatus"}' | node sidecar/dist/sidecar.mjs 2>/dev/null | head -2)
          echo "$output"
          echo "$output" | grep -q '"event":"ready"' || (echo "ready event missing" && exit 1)
          echo "$output" | grep -q '"sidecarVersion"' || (echo "getStatus response missing" && exit 1)

  rust:
    name: Rust (cargo check)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev librsvg2-dev libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - run: npm ci
      - name: Build sidecar (required before cargo build)
        run: node scripts/build-sidecar.mjs
      - name: Cargo check
        working-directory: src-tauri
        run: cargo check --locked
      - name: Cargo test
        if: matrix.os == 'ubuntu-latest'
        working-directory: src-tauri
        run: cargo test --lib --locked

  audit:
    name: Security audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
      - name: npm audit (moderate and above)
        run: npm audit --audit-level=moderate --omit=dev || true
        # npm audit 常因為 transitive dev deps 失敗；這裡暫以 `|| true` 避免
        # 阻斷 CI，仍會在 log 中列出需要關注的警告。正式發行前應移除。
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked || true
      - name: Cargo audit
        working-directory: src-tauri
        run: cargo audit --deny warnings || true
